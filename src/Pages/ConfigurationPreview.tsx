import { Bell, ChevronRight, GitBranch, ListChecks, ShieldCheck, Tags } from 'lucide-react'
import { useState } from 'react'
const sections = [{icon:ShieldCheck,title:'Roles & Permissions',text:'Illustrate possible access for employees, agents, and managers.',value:'Unconfirmed'},{icon:Tags,title:'Ticket Categories',text:'Illustrate how requests could be organized into support areas.',value:'5 examples'},{icon:ListChecks,title:'Ticket Statuses',text:'Illustrate the proposed ticket workflow.',value:'4 examples'},{icon:GitBranch,title:'Assignment Rules',text:'Illustrate how unassigned work could be routed.',value:'Manual'},{icon:Bell,title:'Notifications',text:'Illustrate possible ticket events and delivery preferences.',value:'Unconfirmed'}]
export default function ConfigurationPreview(){const [tab,setTab]=useState('General');return <div className="page"><div className="page-heading"><div><span className="eyebrow">Prototype only</span><h1>Configuration Preview</h1><p>An illustrative view of possible workspace administration.</p></div></div><div className="notice"><ShieldCheck/><div><strong>No confirmed Admin role or permissions</strong><p>These controls are illustrative only. Final roles, categories, statuses, assignment rules, and notifications all require stakeholder confirmation.</p></div></div><div className="settings-layout"><nav className="settings-tabs" aria-label="Configuration preview sections">{['General','People & access','Workflow','Notifications'].map(item=><button key={item} className={tab===item?'active':''} onClick={()=>setTab(item)}>{item}</button>)}</nav><div><section className="panel settings-list"><div className="section-heading"><div><h2>{tab}</h2><p>Explore illustrative configuration concepts for HelpDesk Lite.</p></div></div>{sections.map(({icon:Icon,title,text,value})=><button className="setting-row" key={title}><span className="setting-icon"><Icon/></span><span><strong>{title}</strong><small>{text}</small></span><b>{value}</b><ChevronRight/></button>)}</section></div></div></div>}
