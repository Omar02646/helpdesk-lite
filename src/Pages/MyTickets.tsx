import { Plus } from 'lucide-react'
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router'
import { ticketsApi } from '../api/ticketsApi'
import { TicketFilters } from '../components/tickets/TicketFilters'
import { TicketSummaryCards } from '../components/tickets/TicketSummaryCards'
import { TicketTable } from '../components/tickets/TicketTable'
import { Button } from '../components/ui/Button'
import type { Ticket } from '../types/ticket'
export default function MyTickets(){const navigate=useNavigate();const[allTickets,setAllTickets]=useState<Ticket[]>([]);const[tickets,setTickets]=useState<Ticket[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[search,setSearch]=useState('');const[status,setStatus]=useState('');const[category,setCategory]=useState('');useEffect(()=>{ticketsApi.my().then(data=>{setAllTickets(data);setTickets(data)}).catch(reason=>setError(reason instanceof Error?reason.message:'Unable to load tickets.')).finally(()=>setLoading(false))},[]);useEffect(()=>{const timer=setTimeout(()=>{setLoading(true);ticketsApi.my({search,status:status.replaceAll(' ',''),category}).then(setTickets).catch(reason=>setError(reason instanceof Error?reason.message:'Unable to load tickets.')).finally(()=>setLoading(false))},250);return()=>clearTimeout(timer)},[search,status,category]);return <div className="page"><div className="page-heading"><div><span className="eyebrow">Employee workspace</span><h1>My Tickets</h1><p>View and track all the support tickets you’ve created.</p></div><Button onClick={()=>navigate('/tickets/new')}><Plus size={18}/>Create New Ticket</Button></div><TicketSummaryCards tickets={allTickets}/><section className="panel"><div className="section-heading"><div><h2>Your requests</h2><p>Search, filter, and open a ticket for its latest activity.</p></div></div><TicketFilters {...{search,setSearch,status,setStatus,category,setCategory}}/>{error?<div className="empty-state"><strong>Unable to load tickets</strong><p>{error}</p></div>:loading?<div className="empty-state">Loading tickets…</div>:<TicketTable tickets={tickets}/>}<p className="result-count">Showing {tickets.length} of {allTickets.length} tickets</p></section></div>}
