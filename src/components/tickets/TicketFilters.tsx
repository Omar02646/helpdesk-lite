import { Filter, Search } from 'lucide-react'
import { Select } from '../ui/FormControls'
type Props={search:string;setSearch:(value:string)=>void;status:string;setStatus:(value:string)=>void;category:string;setCategory:(value:string)=>void;owner?:string;setOwner?:(value:string)=>void;showOwner?:boolean;ownerOptions?:{value:string;label:string}[]}
export function TicketFilters({search,setSearch,status,setStatus,category,setCategory,owner,setOwner,showOwner=false,ownerOptions=[]}:Props){return <div className="filters"><label className="search-box"><Search size={18}/><span className="sr-only">Search tickets</span><input value={search} onChange={event=>setSearch(event.target.value)} placeholder="Search tickets..."/></label><Select aria-label="Filter by status" value={status} onChange={event=>setStatus(event.target.value)}><option value="">All statuses</option><option>Open</option><option>In Progress</option><option>In Review</option><option>Resolved</option></Select><Select aria-label="Filter by category" value={category} onChange={event=>setCategory(event.target.value)}><option value="">All categories</option><option>IT Support</option><option>Network</option><option>Email</option><option>Access & Accounts</option></Select>{showOwner?<Select aria-label="Filter by owner" value={owner} onChange={event=>setOwner?.(event.target.value)}><option value="">All owners</option><option value="unassigned">Unassigned</option>{ownerOptions.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</Select>:<button className="secondary-button"><Filter size={16}/>More filters</button>}</div>}
