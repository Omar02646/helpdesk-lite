import { CheckCircle2, CircleDot, Clock3, Tickets } from 'lucide-react'
import type { Ticket } from '../../types/ticket'
import { MetricCard } from '../dashboard/MetricCard'
export function TicketSummaryCards({tickets,counts}:{tickets?:Ticket[];counts?:{all:number;open:number;inProgress:number;resolved:number}}) { const values=counts??{all:tickets?.length??0,open:tickets?.filter(t=>t.status==='Open').length??0,inProgress:tickets?.filter(t=>t.status==='In Progress').length??0,resolved:tickets?.filter(t=>t.status==='Resolved').length??0};return <div className="metrics"><MetricCard label="All Tickets" value={values.all} icon={Tickets}/><MetricCard label="Open" value={values.open} icon={CircleDot} tone="amber"/><MetricCard label="In Progress" value={values.inProgress} icon={Clock3}/><MetricCard label="Resolved" value={values.resolved} icon={CheckCircle2} tone="green"/></div> }
